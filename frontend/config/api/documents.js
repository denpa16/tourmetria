const documents = {
    documents: {
        list: '/api/documents/',
        specs: '/api/documents/specs/',
        facets: '/api/documents/facets/',
        policy: '/api/privacy_policy/'
    }
};

export default documents;
