const progress = {
    progress: {
        list: '/api/progress/',
        id: id => `/api/progress/${id}/`,
        specs: '/api/progress/specs/',
        gallery: '/api/progress/gallery/',
    }
};

export default progress;
